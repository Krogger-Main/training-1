name: Workflow A

on:
  workflow_dispatch
  #push:
    #branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Do some work
        run: echo "🏗️ Building..."

      # …any other steps…
